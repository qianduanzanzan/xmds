<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atxiaoming.mapper.ProdAttrMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.atxiaoming.entity.ProdAttr">
        <result column="id" property="id" />
        <result column="prod_id" property="prodId" />
        <result column="attr_key" property="attrKey" />
        <result column="attr_val" property="attrVal" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        prod_id, attr_key, attr_val
    </sql>

</mapper>
